import React, { useState, useRef } from "react";
// MusicCard archive component removed. Stub exported to prevent build errors
// if any stray imports remain. The real archive components were removed.

const MusicCard = () => null;

export default MusicCard;
